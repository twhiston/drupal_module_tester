#!/usr/bin/env bash

echo "------------------------------------------------------------"
echo "The container is now initialized and running as $HOSTNAME".
echo "Connect to it with this command:"
echo "docker exec -it $HOSTNAME /bin/sh"
echo "------------------------------------------------------------"